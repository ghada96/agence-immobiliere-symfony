{% extends 'base.html.twig' %}

        {% block body %}
            <div class="container mt-4">
                <h1>Créer un bien</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-danger">Enregistrer</button>
                {{ form_end(form) }}


            </div>

        {% endblock %}



